{
  "name": "@endge/chrome-extension",
  "version": "0.1.1",
  "private": false,
  "license": "Apache-2.0",
  "type": "module",
  "files": [
    "dist",
    "src",
    "static",
    "scripts",
    "tsconfig.json"
  ],
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "build": "pnpm run clean && tsc -p tsconfig.json && node ./scripts/prepare-dist.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
